---
artifact_type: repo_assessment
status: draft
governance_role: overall_repo_assessment
run_id: RUN-0017
trigger: manual_request
workflow: repo_working_assessment
constitutional: false
---

# RUN-0017: How The Repo Is Working So Far

## Short Verdict

The repo is working. It is no longer just scaffold. It has started doing the job: weakening claims, killing weak readings, narrowing formal objects, creating test artifacts, and preserving the path by which decisions were made.

The strongest concern is that the surviving idea may be absorbed by time-as-finality-style gluing and record readout. That is a good failure mode because it is now specific enough to test.

## What Is Working

- The thin trigger works as intended: W000 owns routing, not the automation.
- The steward preserves memory and provenance well.
- Claim statuses are moving instead of staying protected.
- The repo did not expand its constitution.
- The run nomenclature problem was corrected without rewriting history.
- The steward can now parallelize separable research lanes.
- The repo has moved from hypothesis prose to formal pressure and toy tests.

## What Is Not Working Yet

- The project still has too many governance artifacts relative to completed formal tests.
- Source review is only at readiness level.
- Cross-repo context is newly added and untested.
- The strongest survivor may not be Temporal Issuance at all; it may be time-as-finality readout plus gluing obstruction.
- There is no executable model yet.

## Research State

Current best survivor:

```text
partial reconciliation and obstruction across local patches:
G_ij plus Omega_ij
```

Weak survivor:

```text
kappa_i as ordinal update relation
```

Currently outside core:

```text
lambda_i / mu as issuance measure
```

Killed:

- global `dR` as universal moving frontier
- generic `mu` as monotone issuance amount

## Governance State

The governance architecture is good enough for now.

Do not add:

- more constitutional objects
- full VSM manual
- voting or contributor rights
- permanent persona hierarchy
- hard prioritization formula
- full schema validation

Use the future-run queue instead.

## Steward Behavior

The steward was initially too cautious and too serial. That changed in RUN-0010. The recent sequence shows healthier behavior:

- parallel lanes where separable
- hard outputs
- claim weakening
- path kill
- toy model
- bounded urgent fixes

The steward should now bias toward more formal tests and fewer assessment passes.

## Best Next Work

Build the `issuance_to_finality` bridge toy model.

It should test:

- same issuance, different cadence
- same observer records, different hidden issuance
- same issuance and records, different measure candidate
- nonmonotone access loss
- gluing failure

If this collapses into time-as-finality, record absorption. If it preserves source-side distinctions that TaF readout loses, Temporal Issuance has a sharper survivor.

## Open Questions For Joe

1. Should the next run prioritize the `issuance_to_finality` bridge model?
2. Should time-as-finality extraction happen before or during that model?
3. Should GU formalization's six-axis protocol be adapted after the next model, or only if no-go pressure blocks progress?

## Bottom Line

Temporal Issuance is now a real research-governance repo, not just a concept scaffold.

It has not justified the launch hypothesis.

It has created a narrower and more testable survivor:

```text
local realization patches plus reconciliation obstruction
```

The next decisive question is whether that survivor belongs to Temporal Issuance or is absorbed by time-as-finality.
