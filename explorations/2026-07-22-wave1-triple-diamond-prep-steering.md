---
title: "Directed-wake steering — Wave-1 triple-diamond prep (TI's three high-value items)"
status: steering-input
doc_type: directed-wake-steering
created: 2026-07-22
source: Wave-1 triple-diamond prep (Joe-directed); full portfolios in the wave-1 workflow output
boundary: steering input for the hourly cadence / steward. No claim/canon/verdict/freeze moved. Prep only — falsifiers predeclared; evidence disposes.
---

# TI — directed-wake steering (Wave-1 prep)

**Cross-repo frame:** TI-1 is the SHARPEST statement of a single self-generating-source object shared with
P2C-2 and TaF-1 — the **computable-pre-commitment** face: *is the next admissible step a computable function
of the finite record?* YES → SSC-reproducible disclosure (absorbed); NO → genuine issuance. TI-3 is the
**separate σ/orientation axis** and has **NO bearing on the D-FORK** (TI's real North Star) — do not let
σ-progress read as source-progress. Guard: TI-1 advances NOW on native fixture; **do NOT re-park it "waiting
on a source-issued packet"** (the eternal block it was designed to escape).

## TI-1 — Gödelian operative source OR impossibility  · FRONTIER (portfolio ready; best-armed AFTER TI-2)
- **Reframe:** the abstract math is DONE (D-FORK/E042). TI-1 is ONE dichotomy: does PHYSICAL realizability
  force the finite/computable regime (→ clean falsification N1 = SUCCESS), or can a physical source sit on
  the productive/Gödelian side (→ first real Gödelian source)? **Stop scanning emergent-behavior domains**
  (all 14 deaths shared `source_growth_core_passed:false`); attack the **finite-stage face** directly.
- **Wake directive:** run the finite-stage-operativity fixture on **already-undecidable physics**
  (Cubitt-Perez-Garcia-Wolf spectral-gap / Wang-tile admissibility): decide **c.e.-at-finite-stage** (→ N1
  falsification = SUCCESS) vs **productive-via-self-reference** (→ construction C3/C1 → six-criteria +
  Adapter_P packet). Predeclare the total-computable next-step certificate as the kill (it simultaneously
  advances N1). Two-sided: both outcomes bank on the North Star.
- **Grade:** recommended C3∩N1 pair is `gated`; the **N1 falsification leg is doable-now**. Definitive-
  verdict odds ~15–25% within a bounded campaign — but the falsification side is equally a SUCCESS.

## TI-2 — D-FORK disclosure-adversary hardening (E180)  · DOABLE-NOW, writing-only · FEEDS TI-1
- **Wake directive:** draft **E180**: theorem-inventory table + grades + dependency graph for E041/E042/E045;
  the oracle-strength-parameterised disclosure-adversary class; the kill-cond-2 verdict (conditional-theorem-
  against-oracle-bounded-adversaries + degree-characterization; **oracle-boundedness = an L5 posit, NOT
  proved**). **Ship as a TI-1 INPUT, not a claim promotion.** Its oracle-boundedness location tells TI-1
  exactly what the physical source must posit — **sequence TI-2 before the hard TI-1 swing.**
- **Discipline:** Track-2 byproduct; must FEED TI-1, never be mistaken for the North Star.

## TI-3 — domain-α-even bridge + Lean reduction (σ axis)  · scaffold DOABLE-NOW; bridge GATED
- **Wake directive:** open `BoundaryDomainAction.lean`: define the domain α-action ρ on `Read` +
  `EquivariantReader`, **STATE** (not prove) `equivariant_reader_is_sigma_blind` with `C_read`-origin and
  Kramers-evenness as named True-hypotheses, prove the orbit-constant `_skeleton` with no `sorry`; in
  parallel serialize the lake build of the two existing modules. **Do NOT touch O-b, D-FORK, or any TI-C
  claim.**
- **Grade:** scaffold (STATE-only) doable-now; the load-bearing **bridge is gated** on GU's `C_read` domain
  action + the Kramers T²=−1 posit. Dominant failure mode = **circularity** (defining "internal" as "even");
  the internality⇒equivariance derivation is the gated part and must be derived, not assumed. σ settles
  orientation/polarity only.

**Full candidate portfolios + predeclared falsifiers:** Wave-1 triple-diamond prep output (2026-07-22).
