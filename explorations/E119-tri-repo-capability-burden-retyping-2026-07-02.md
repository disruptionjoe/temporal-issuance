---
artifact_type: exploration
status: active
governance_role: cross_repo_distillation
constitutional: false
claim_status_change: none
---

# E119 Tri-Repo Capability Burden Retyping

## Purpose

Distill the TaF / GU / TI synthesis in
`time-as-finality/audits/2026-07-01-tri-repo-synthesis-hegelian-metasynthesis.md`
into Temporal Issuance without importing claims or changing the core
hypothesis.

## Local Takeaway

`FORMAL-DEFINITION-REPAIR.md` says the next `Ext_S` test is:

```text
Test whether any source extension rule changes observer capabilities in a way
time-as-finality readout cannot already express.
```

The synthesis and TaF T392/T393/T395 sharpen that burden. A statistics-only
comparison is no longer the right target. TaF has finite evidence that a record
accessibility difference can leave the declared reduced process statistics
unchanged while changing what the holder can do or postdict. The TI burden
therefore has to be phrased against capability-typed readout, not against an
older statistics-typed readout.

Updated working burden:

```text
Find an Ext_S rule that changes a region-indexed observer task menu in a way
TaF's capability-typed readout cannot express; otherwise archive the surplus
as readout/projection/finality vocabulary.
```

## What This Does Not Do

- Does not promote `Ext_S`.
- Does not reopen archived energy-momentum routes.
- Does not move TI-C019, TI-C020, or TI-C022.
- Does not claim TaF proves or refutes Temporal Issuance.
- Does not use GU as support.

## Consequences For Next Fixtures

1. **Capability-typed Ext_S test.** Re-run the `Ext_S` surplus question with a
   declared observer/region, a task menu, and a capability comparison, not just
   process statistics.
2. **Multi-holder third-axis test.** The driving hypothesis already names the
   next observer-side step: rising reversal cost across distinct holders. The
   synthesis adds that T392/T393 provide record-side equality and boundary
   certificates that can be reused by the fixture.
3. **COMP repair first.** The multi-holder axis should not claim orthogonality
   to COMP until the computational-irreducibility leg is repaired or
   de-correlated from the Rule-110 universality caveat.
4. **H1 / descent route.** The Cech/H1 fixture is relevant to K4 only as a
   fixture-class counterweight. It does not establish that physical observer
   ensembles have nontrivial H1.

## Collapse Criteria

Archive the `Ext_S` surplus reading if:

- every candidate capability change is just boundary relocation already
  expressible by TaF D1-relative-to-access;
- the task menu is not observer/region-supported;
- the separation appears only after smuggling in a hidden source datum;
- the effect is absorbed by fixed-source aperture disclosure, fixed site,
  fixed observable algebra, or ordinary intervention/state completion.

## Candidate Route Name

```text
capability_typed_Ext_S_surplus_test
```

This is optional route material. It does not override the current
`OnlineIssuance^LC` next-trigger plan.
