# TI-PIT-02 — Projection-vs-Admissibility Paired Intervention Case, draft v0.2

**Candidate packet ID:** `TI-PIT-02` (paired-intervention test, second source case)
**Status:** `DRAFT-FOR-SOURCE-ISSUANCE` v0.2 — authored content proposed *to* the
Temporal Issuance repository. It is not a source-issued frozen packet until the
TI steward adopts, revises, pins, and issues it under a packet schema version.
Under the frozen-packet rule, the receiver must treat this case as
`NOT_YET_IMPORTABLE` until TI issues it.
**Supersedes:** the TI-PIT-02 draft embedded in
`possibility-to-capability/explorations/2026-07-16-northstar-unblock/lane-C-frame-r-and-ti-case.md`
(v0.1), returned by TI as NOT-YET-ISSUABLE on 2026-07-16 with four named
blocking deficiencies. This revision exists to cure exactly those deficiencies;
the fixture's mathematical content (systems, interventions, computed facts) is
unchanged except where a deficiency required re-specification.
**Author:** possibility-to-capability (re-authoring run, 2026-07-16). TI declined
to re-author, correctly, so authorship and provenance stay with P2C.
**Proposed TI path (TI owns final placement):**
`temporal-issuance/interfaces/possibility-to-capability/TI-PIT-02-proj-vs-adm-paired-intervention-v0.2.md`

**Source-sovereignty statement:** Temporal Issuance owns all definitions used
here (`OnlineIssuance^LC` components per `FORMAL-OBJECT.md` RUN-0081) and owns
the fixture, its evidence, and its claim status upon issuance. Nothing in this
packet moves, reopens, or reinterprets any TI claim: `TI-C019` and `TI-C020`
status untouched; `Issue[S]^physical` remains `false`. The fixture below is a
finite toy instance, not evidence about physical issuance. The receiver may
classify; it may not upgrade.

**Frame-blindness statement:** this packet cites, scores, and advocates no
normalization frame, receiver frame, or rival frame. It is a TI-native paired
intervention case only. All provenance facts formerly stated with frame
citations are restated below in frame-neutral language.

**Authorship restriction (quarantine, carried from the governing referee):**
the lane that authored this fixture's v0.1 also authored a receiver-side rival
normalization frame in a separate artifact. This packet and any frame authored
by that lane may not be used together as a frame-discriminating pair in any
receiver adjudication; the receiver's independent-reviewer and blind-issuer
requirements must each be satisfied by parties other than that lane. This
restriction survives issuance.

**Reproducible artifact (in hand):** the complete executable fixture is inlined
verbatim in §2.3 and is byte-identical to the committed file
`possibility-to-capability/explorations/2026-07-16-tipit02-reissuance/ti_pit_02_fixture.py`
(10371 bytes, LF line endings).

- `content_sha256(ti_pit_02_fixture.py)` = `8888afc78847a2f9930ed453e2a1e16b62b2e647542cb23cc99eb15230ea4bed`
- `content_sha256(§2.4 output block, LF)` = `a14642b7cd27bcce510bbe28f5112d93b13db415434ae2f3d416048ec4a62475`

The §2.4 output is the true, unedited, unabridged stdout of one run of that
exact script (Python 3, stdlib only, deterministic; exit code 0). Nothing is
condensed. The packet file itself is generated by a builder that splices the
script and output in from the artifact bytes, so the displayed code is the code
that produced the displayed output by construction.

## 2.1 Exact small objects: native RUN-0081 signature

The source object is TI's seven-component signature, quoted exactly from
`temporal-issuance/FORMAL-OBJECT.md` (RUN-0081):

```text
OnlineIssuance^LC =
(
  Gamma_n,     prefix-presented constructive context
  Adm_n,       admissibility predicate formed at prefix n
  Ext_n,       witness-bearing extension category available at n
  Iss_n,       source extension step
  Proj_{o,n},  observer projection/readout
  Glue_n,      downstream reconciliation/finality operation
  tau_n        recordable source trace
)
```

Instantiation map — which components this finite fixture instantiates and which
it trivializes (declared, not silently dropped):

| Component | Status | Instantiation |
|---|---|---|
| `Gamma_n` | instantiated | set of issued tokens (binary strings over {a, b}); `Gamma_0 = {"a"}` |
| `Adm_n` | instantiated | `Adm_k(e) <=> e ∉ Gamma ∧ |e| ≤ 4 ∧ #b(e) ≤ k` (admissibility parameter k) |
| `Ext_n` | trivialized | represented by the finite set of pairs `(e, w)` with `e ∈ CandExt(Gamma_n) = { t·c : t ∈ Gamma_n, c ∈ {a,b} }` (the gate-clause candidate object) and `w` an explicit admissibility witness record; no nontrivial categorical structure (morphisms, composition) is exercised |
| `Iss_n` | instantiated | at stage n, issue *all* admissible candidates in sorted order, deterministically; `Gamma_{n+1} = Gamma_n ∪ issued`; stages n = 1..4 |
| `Proj_{o,n}` | instantiated | length-threshold readout: the observer receives exactly the trace events whose token length ≤ L; `Proj_full` = identity |
| `Glue_n` | present, not exercised | single observer; reconciliation is identity. Declared as unexercised rather than dropped from the signature |
| `tau_n` | instantiated | ordered event list `(stage, token, w)` with the admissibility witness `w` carried as an explicit per-event field |

Witnesses (RUN-0081 gate clause 2, `w_n : Adm_n(e_n)`): every issuance event in
`tau_n` carries the explicit witness field `w = (novel, length, b_count)`, a
function of `(e, Gamma)` alone. For parameter k, `w` certifies `Adm_k(e)`
exactly when `novel ∧ length ≤ 4 ∧ b_count ≤ k`. Because `w` is k-free, carrying
it through the observer projection leaks no information about k, and the
hypothesis-consistency analysis below is unchanged from v0.1.

Shared task vocabulary `V` (issuance/reconstruction tasks over `Voc` = all 30
binary strings of length 1–4):

- **ISS(s):** "is s issued by stage 4?" — a source-side issuance fact.
- **REC(s):** "does the observer's projected record *determine* ISS(s)?" — a
  reconstruction fact. Reconstruction model (declared, load-bearing): the
  observer knows `Gamma_0`, `CandExt`, `Iss`, the stage count, the projection
  rule, and that `Adm = Adm_k` for some k in the hypothesis class
  `H = {0,1,2,3}`; the observer does not know k. REC(s) is decided iff every
  k ∈ H whose projected trace equals the observed one agrees on ISS(s).

## 2.2 The paired intervention

- **BASE:** k = 1, projection `Proj_{L=2}`.
- **ALPHA (projection-side intervention):** change *only* `Proj`:
  `Proj_{L=2} → Proj_full`. Issuance structure (`Gamma_0`, `CandExt`, `Adm_1`,
  `Iss`) bit-identical to BASE.
- **BETA (admissibility-side intervention):** change *only* `Adm`:
  `Adm_1 → Adm_2`, so candidate extensions containing two b's become issuable.
  Projection bit-identical to BASE (`Proj_{L=2}`).
- **Supplementary fixed-projection pair (BASE′/BETA′):** the same two systems
  under the fixed wider projection `Proj_{L=3}`, included so the record carries
  a regime where the admissibility intervention *is* observer-visible
  (visibility of BETA under `Proj_{L=2}` is a parameter fact, not a structural
  necessity).

No leg is labeled with any receiver classification here. The packet reports
computed before/after facts only; classification belongs to the receiver's own
preregistered gate machinery, outside this packet.

## 2.3 Machine-checkable computation (complete, inlined verbatim)

The block below is byte-identical to the committed artifact
`ti_pit_02_fixture.py` (`sha256 = 8888afc78847a2f9930ed453e2a1e16b62b2e647542cb23cc99eb15230ea4bed`). It contains the check
registry with [T]/[E]/[F] tags: [T] checks hold by construction and carry no
evidential weight; [E] checks are genuine experiments; [F] checks are
failing-direction controls that must fail, proving on the same code path that
the checker each protects has teeth. The fixture lints clean under the
receiving repo's `tests/tef_check_tag_linter.py --strict`.

```python
#!/usr/bin/env python3
"""TI-PIT-02 paired-intervention fixture, draft v0.2 (DRAFT-FOR-SOURCE-ISSUANCE).

Finite instance of Temporal Issuance's OnlineIssuance^LC signature, quoted
exactly from TI FORMAL-OBJECT.md RUN-0081:

    OnlineIssuance^LC =
    (
      Gamma_n,     prefix-presented constructive context
      Adm_n,       admissibility predicate formed at prefix n
      Ext_n,       witness-bearing extension category available at n
      Iss_n,       source extension step
      Proj_{o,n},  observer projection/readout
      Glue_n,      downstream reconciliation/finality operation
      tau_n        recordable source trace
    )

Instantiation map (which components are instantiated vs trivialized):
  Gamma_n     INSTANTIATED: set of issued tokens; Gamma_0 = {"a"}.
  Adm_n       INSTANTIATED: Adm_k(e) <=> e not in Gamma and |e| <= MAXLEN
              and #b(e) <= k (admissibility parameter k).
  Ext_n       TRIVIALIZED: the witness-bearing extension category is
              represented by the finite set of pairs (e, w) with
              e in CandExt(Gamma_n) = { t+c : t in Gamma_n, c in {a,b} }
              (the gate-clause candidate object) and w an explicit
              admissibility witness record; no nontrivial categorical
              structure (morphisms, composition) is exercised.
  Iss_n       INSTANTIATED: issue ALL admissible candidates, deterministic,
              sorted order.
  Proj_{o,n}  INSTANTIATED: length-threshold readout; the observer receives
              exactly the trace events whose token length <= L. Witness
              fields are functions of the token and prior context alone
              (k-free), so carrying them through the projection leaks
              nothing about k.
  Glue_n      PRESENT IN THE SIGNATURE, NOT EXERCISED: single observer;
              reconciliation is identity. Declared, not dropped.
  tau_n       INSTANTIATED: ordered event list (stage, token, w) with the
              admissibility witness w carried as an explicit per-event
              field (RUN-0081 gate clause 2: w_n : Adm_n(e_n)).

Check discipline: every check is tagged [T]/[E]/[F] per the receiving repo's
house rule (tests/tef_check_tag_linter.py). [T] = holds by construction, no
evidential weight; [E] = genuine experiment; [F] = failing-direction control
proving the checker it protects has teeth on the same code path.
"""

from itertools import product

ALPH = "ab"
N_STAGES = 4
MAXLEN = 4
HYP = [0, 1, 2, 3]


def witness(e, gamma):
    """k-free admissibility witness data for candidate e at the current prefix.

    w = (novel, length, b_count). For parameter k, w certifies Adm_k(e)
    exactly when novel and length <= MAXLEN and b_count <= k. The record is
    a function of (e, gamma) alone, so it carries no information about k.
    """
    return (e not in gamma, len(e), e.count("b"))


def run_system(k, n_stages=N_STAGES, maxlen=MAXLEN):
    """Deterministic issuance run; trace events are (stage, token, w)."""
    gamma = {"a"}
    trace = []
    stage_counts = []
    for n in range(1, n_stages + 1):
        cands = sorted({t + c for t in gamma for c in ALPH})
        issued = []
        for e in cands:
            w = witness(e, gamma)
            novel, length, b_count = w
            if novel and length <= maxlen and b_count <= k:
                issued.append((e, w))
        trace.extend((n, e, w) for (e, w) in issued)
        stage_counts.append(len(issued))
        gamma |= {e for (e, _) in issued}
    return {e for (_, e, _) in trace}, trace, stage_counts


def project(trace, L):
    """Observer readout Proj_L: trace events whose token length <= L.

    L is None for the full (identity) projection."""
    if L is None:
        return list(trace)
    return [(n, e, w) for (n, e, w) in trace if len(e) <= L]


def toklen(s):
    return (len(s), s)


VOC = sorted(("".join(p) for l in range(1, MAXLEN + 1)
              for p in product(ALPH, repeat=l)), key=toklen)
SYS = {k: run_system(k) for k in HYP}


def analyze(true_k, L):
    """Observer analysis: reconstruction model per packet section 2.1.

    The observer knows Gamma_0, CandExt, Iss, the stage count, the
    projection rule, and that Adm = Adm_k for some k in HYP; it does not
    know k. A task ISS(s) is decidable iff every k in HYP whose projected
    trace equals the observed one agrees on ISS(s)."""
    obs = project(SYS[true_k][1], L)
    consistent = [k for k in HYP if project(SYS[k][1], L) == obs]
    decid = [s for s in VOC if len({s in SYS[k][0] for k in consistent}) == 1]
    return {"issued": sorted(SYS[true_k][0], key=toklen),
            "record": obs, "consistent_k": consistent, "decidable": decid}


CONDS = {
    "BASE  (k=1, L=2)": (1, 2),
    "ALPHA (k=1, full)": (1, None),
    "BETA  (k=2, L=2)": (2, 2),
    "BASE' (k=1, L=3)": (1, 3),
    "BETA' (k=2, L=3)": (2, 3),
}
R = {name: analyze(*args) for name, args in CONDS.items()}


def fmt_events(events):
    return "[" + ", ".join(
        "({0},{1!r},w=({2},{3},{4}))".format(n, e, int(w[0]), w[1], w[2])
        for n, e, w in events) + "]"


def fmt_set(tokens):
    return "[" + ", ".join(repr(t) for t in sorted(tokens, key=toklen)) + "]"


def same_record(k_a, L_a, k_b, L_b):
    return project(SYS[k_a][1], L_a) == project(SYS[k_b][1], L_b)


RESULTS = []


def check(name, value, expected=True):
    RESULTS.append((name, bool(value), expected))


def main():
    print("TI-PIT-02 fixture v0.2 (draft for TI source issuance)")
    print("=" * 72)
    for name, res in R.items():
        print("== " + name)
        print("   IssuedSet ({0} tokens): {1}".format(
            len(res["issued"]), fmt_set(res["issued"])))
        print("   ProjRecord ({0} events): {1}".format(
            len(res["record"]), fmt_events(res["record"])))
        print("   consistent k under HYP: {0}".format(res["consistent_k"]))
        print("   DecidableSet ({0} of {1}): {2}".format(
            len(res["decidable"]), len(VOC), fmt_set(res["decidable"])))
    print()
    print("== Pairwise deltas (added, removed)")
    for label, (a, b) in {
        "BASE  -> ALPHA": ("BASE  (k=1, L=2)", "ALPHA (k=1, full)"),
        "BASE  -> BETA ": ("BASE  (k=1, L=2)", "BETA  (k=2, L=2)"),
        "BASE' -> BETA'": ("BASE' (k=1, L=3)", "BETA' (k=2, L=3)"),
    }.items():
        ia, ib = set(R[a]["issued"]), set(R[b]["issued"])
        da, db = set(R[a]["decidable"]), set(R[b]["decidable"])
        print(" {0}: issued +{1} -{2}".format(
            label, fmt_set(ib - ia), fmt_set(ia - ib)))
        print(" {0}: decidable +{1} -{2}".format(
            " " * len(label), fmt_set(db - da), fmt_set(da - db)))
        print(" {0}: projRecord identical: {1}".format(
            " " * len(label), R[a]["record"] == R[b]["record"]))
    print()
    print("== Checks ([T] no evidential weight; [E]/[F] evidential)")

    base = R["BASE  (k=1, L=2)"]
    alpha = R["ALPHA (k=1, full)"]
    beta = R["BETA  (k=2, L=2)"]
    basep = R["BASE' (k=1, L=3)"]
    betap = R["BETA' (k=2, L=3)"]

    check("[T] alpha_iss: ALPHA leaves the source issuance set fixed"
          " -- holds by construction of the fixture (the issued set is a"
          " function of k alone and both legs use k=1); theorem-consequence,"
          " no evidential weight",
          set(base["issued"]) == set(alpha["issued"]))
    check("[F] alpha_iss-fail: the same set comparison detects a genuine"
          " issuance change under the perturbed pairing k=1 vs k=2",
          set(base["issued"]) == set(beta["issued"]), expected=False)

    check("[E] beta_rec: BETA leaves the L=2 projected record fixed"
          " (the admissibility change is invisible through Proj_{L=2})",
          same_record(1, 2, 2, 2))
    check("[F] beta_rec-fail: the same record comparison at the same"
          " threshold L=2 detects the perturbed pairing k=0 vs k=1"
          " (event (1,'ab') present only for k>=1)",
          same_record(0, 2, 1, 2), expected=False)

    check("[E] beta_dec: BETA leaves the L=2 observer decidable set fixed",
          base["decidable"] == beta["decidable"])
    check("[F] beta_dec-fail: the same decidable-set comparison flips at the"
          " perturbed threshold L=3 ('abbb' becomes undecidable)",
          basep["decidable"] == betap["decidable"], expected=False)

    check("[E] betap_rec: BETA' changes the L=3 projected record (the same"
          " admissibility change is observer-visible under the wider fixed"
          " projection)",
          not same_record(1, 3, 2, 3))
    check("[F] betap_rec-fail: the same difference test reports no change on"
          " the identity pairing k=1 vs k=1 at L=3",
          not same_record(1, 3, 1, 3), expected=False)

    ext = {k: run_system(k, n_stages=6) for k in HYP}
    check("[E] quiesce: an extended-horizon run (6 stages) issues zero events"
          " at stages 4-6 for every k in HYP -- machine-checked quiescence"
          " (trivially provable from the MAXLEN bound; proof not written)",
          all(ext[k][2][3:] == [0, 0, 0] for k in HYP))
    check("[F] quiesce-fail: the same stage-count predicate fails at stage 3,"
          " where issuance events do occur",
          all(ext[k][2][2] == 0 for k in HYP), expected=False)
    check("[F] quiesce_bound-fail: perturbing the length bound to MAXLEN=5"
          " produces issuance events at stage 4, where the unperturbed"
          " fixture is quiescent (same counting code path; protects:"
          " quiesce)",
          run_system(1, n_stages=6, maxlen=5)[2][3] == 0, expected=False)

    n_t = n_e = n_f = 0
    failures = []
    for name, value, expected in RESULTS:
        ok = value == expected
        n_t += name.startswith("[T]")
        n_e += name.startswith("[E]")
        n_f += name.startswith("[F]")
        print("{0}  {1}: {2}".format("PASS" if ok else "UNEXPECTED",
                                     name, value))
        if not ok:
            failures.append(name)
    print()
    print("EVIDENTIAL CHECKS: {0} [E] + {1} [F] = {2}; [T]"
          " theorem-consequence checks (no evidential weight): {3}".format(
              n_e, n_f, n_e + n_f, n_t))
    if failures:
        print("UNEXPECTED RESULTS: {0}".format(failures))
        return 1
    print("All checks match expectations. Exit 0.")
    return 0


if __name__ == "__main__":
    import sys
    sys.exit(main())
```

## 2.4 Computed output (verbatim, byte-level)

True output of one run of the inlined script (`python ti_pit_02_fixture.py`), unedited and unabridged. sha256 of this block's LF-normalized bytes: `a14642b7cd27bcce510bbe28f5112d93b13db415434ae2f3d416048ec4a62475`.

```text
TI-PIT-02 fixture v0.2 (draft for TI source issuance)
========================================================================
== BASE  (k=1, L=2)
   IssuedSet (9 tokens): ['aa', 'ab', 'aaa', 'aab', 'aba', 'aaaa', 'aaab', 'aaba', 'abaa']
   ProjRecord (2 events): [(1,'aa',w=(1,2,0)), (1,'ab',w=(1,2,1))]
   consistent k under HYP: [1, 2, 3]
   DecidableSet (25 of 30): ['a', 'b', 'aa', 'ab', 'ba', 'bb', 'aaa', 'aab', 'aba', 'baa', 'bab', 'bba', 'bbb', 'aaaa', 'aaab', 'aaba', 'abaa', 'baaa', 'baab', 'baba', 'babb', 'bbaa', 'bbab', 'bbba', 'bbbb']
== ALPHA (k=1, full)
   IssuedSet (9 tokens): ['aa', 'ab', 'aaa', 'aab', 'aba', 'aaaa', 'aaab', 'aaba', 'abaa']
   ProjRecord (9 events): [(1,'aa',w=(1,2,0)), (1,'ab',w=(1,2,1)), (2,'aaa',w=(1,3,0)), (2,'aab',w=(1,3,1)), (2,'aba',w=(1,3,1)), (3,'aaaa',w=(1,4,0)), (3,'aaab',w=(1,4,1)), (3,'aaba',w=(1,4,1)), (3,'abaa',w=(1,4,1))]
   consistent k under HYP: [1]
   DecidableSet (30 of 30): ['a', 'b', 'aa', 'ab', 'ba', 'bb', 'aaa', 'aab', 'aba', 'abb', 'baa', 'bab', 'bba', 'bbb', 'aaaa', 'aaab', 'aaba', 'aabb', 'abaa', 'abab', 'abba', 'abbb', 'baaa', 'baab', 'baba', 'babb', 'bbaa', 'bbab', 'bbba', 'bbbb']
== BETA  (k=2, L=2)
   IssuedSet (13 tokens): ['aa', 'ab', 'aaa', 'aab', 'aba', 'abb', 'aaaa', 'aaab', 'aaba', 'aabb', 'abaa', 'abab', 'abba']
   ProjRecord (2 events): [(1,'aa',w=(1,2,0)), (1,'ab',w=(1,2,1))]
   consistent k under HYP: [1, 2, 3]
   DecidableSet (25 of 30): ['a', 'b', 'aa', 'ab', 'ba', 'bb', 'aaa', 'aab', 'aba', 'baa', 'bab', 'bba', 'bbb', 'aaaa', 'aaab', 'aaba', 'abaa', 'baaa', 'baab', 'baba', 'babb', 'bbaa', 'bbab', 'bbba', 'bbbb']
== BASE' (k=1, L=3)
   IssuedSet (9 tokens): ['aa', 'ab', 'aaa', 'aab', 'aba', 'aaaa', 'aaab', 'aaba', 'abaa']
   ProjRecord (5 events): [(1,'aa',w=(1,2,0)), (1,'ab',w=(1,2,1)), (2,'aaa',w=(1,3,0)), (2,'aab',w=(1,3,1)), (2,'aba',w=(1,3,1))]
   consistent k under HYP: [1]
   DecidableSet (30 of 30): ['a', 'b', 'aa', 'ab', 'ba', 'bb', 'aaa', 'aab', 'aba', 'abb', 'baa', 'bab', 'bba', 'bbb', 'aaaa', 'aaab', 'aaba', 'aabb', 'abaa', 'abab', 'abba', 'abbb', 'baaa', 'baab', 'baba', 'babb', 'bbaa', 'bbab', 'bbba', 'bbbb']
== BETA' (k=2, L=3)
   IssuedSet (13 tokens): ['aa', 'ab', 'aaa', 'aab', 'aba', 'abb', 'aaaa', 'aaab', 'aaba', 'aabb', 'abaa', 'abab', 'abba']
   ProjRecord (6 events): [(1,'aa',w=(1,2,0)), (1,'ab',w=(1,2,1)), (2,'aaa',w=(1,3,0)), (2,'aab',w=(1,3,1)), (2,'aba',w=(1,3,1)), (2,'abb',w=(1,3,2))]
   consistent k under HYP: [2, 3]
   DecidableSet (29 of 30): ['a', 'b', 'aa', 'ab', 'ba', 'bb', 'aaa', 'aab', 'aba', 'abb', 'baa', 'bab', 'bba', 'bbb', 'aaaa', 'aaab', 'aaba', 'aabb', 'abaa', 'abab', 'abba', 'baaa', 'baab', 'baba', 'babb', 'bbaa', 'bbab', 'bbba', 'bbbb']

== Pairwise deltas (added, removed)
 BASE  -> ALPHA: issued +[] -[]
               : decidable +['abb', 'aabb', 'abab', 'abba', 'abbb'] -[]
               : projRecord identical: False
 BASE  -> BETA : issued +['abb', 'aabb', 'abab', 'abba'] -[]
               : decidable +[] -[]
               : projRecord identical: True
 BASE' -> BETA': issued +['abb', 'aabb', 'abab', 'abba'] -[]
               : decidable +[] -['abbb']
               : projRecord identical: False

== Checks ([T] no evidential weight; [E]/[F] evidential)
PASS  [T] alpha_iss: ALPHA leaves the source issuance set fixed -- holds by construction of the fixture (the issued set is a function of k alone and both legs use k=1); theorem-consequence, no evidential weight: True
PASS  [F] alpha_iss-fail: the same set comparison detects a genuine issuance change under the perturbed pairing k=1 vs k=2: False
PASS  [E] beta_rec: BETA leaves the L=2 projected record fixed (the admissibility change is invisible through Proj_{L=2}): True
PASS  [F] beta_rec-fail: the same record comparison at the same threshold L=2 detects the perturbed pairing k=0 vs k=1 (event (1,'ab') present only for k>=1): False
PASS  [E] beta_dec: BETA leaves the L=2 observer decidable set fixed: True
PASS  [F] beta_dec-fail: the same decidable-set comparison flips at the perturbed threshold L=3 ('abbb' becomes undecidable): False
PASS  [E] betap_rec: BETA' changes the L=3 projected record (the same admissibility change is observer-visible under the wider fixed projection): True
PASS  [F] betap_rec-fail: the same difference test reports no change on the identity pairing k=1 vs k=1 at L=3: False
PASS  [E] quiesce: an extended-horizon run (6 stages) issues zero events at stages 4-6 for every k in HYP -- machine-checked quiescence (trivially provable from the MAXLEN bound; proof not written): True
PASS  [F] quiesce-fail: the same stage-count predicate fails at stage 3, where issuance events do occur: False
PASS  [F] quiesce_bound-fail: perturbing the length bound to MAXLEN=5 produces issuance events at stage 4, where the unperturbed fixture is quiescent (same counting code path; protects: quiesce): False

EVIDENTIAL CHECKS: 4 [E] + 6 [F] = 10; [T] theorem-consequence checks (no evidential weight): 1
All checks match expectations. Exit 0.
```

**Structural summary of the dissociation (facts, not labels):**

- **ALPHA:** source issuance set exactly fixed (9 tokens; this identity holds
  by construction and is tagged [T], carrying no evidential weight — the issued
  set is a function of k alone and both legs use k=1); the observer's decidable
  set grows 25 → 30; the five newly decidable tasks
  (`abb, aabb, abab, abba, abbb`) are all decided *negative* — tokens the fixed
  structure never issues.
- **BETA:** source issuance set grows 9 → 13; each of the four new issued
  extensions carries its explicit trace witness (e.g. `('abb', w=(1,3,2))`:
  novel, length 3 ≤ 4, b_count 2 ≤ k=2); the observer's projected record and
  entire decidable set are bit-for-bit unchanged — through the fixed L=2
  projection, BETA is invisible.
- **BETA′ (fixed wider projection):** the same admissibility change is
  observer-visible (`abb` enters the record at stage 2) and *shrinks*
  reconstruction decidability by one task (`abbb` becomes undecidable:
  consistent hypotheses {2,3} disagree on it) while enlarging issuance. An
  admissibility-side intervention can simultaneously enlarge what is issued and
  reduce what is reconstructible.

**Failing-direction controls (perturbation probes):** the run carries 6 [F]
controls alongside 4 [E] checks: a perturbed pairing (k=1 vs k=2) that the
issuance-set comparison must detect; a perturbed pairing (k=0 vs k=1) that the
record comparison must detect at the same threshold L=2; a perturbed threshold
(L=3) that must flip the decidable-set comparison; an identity pairing the
difference test must not fire on; the quiescence predicate applied at stage 3,
where it must fail; and a perturbed length bound (MAXLEN=5) that must produce
events at stage 4. All six fail as required (exit 0 requires it), so every
substantive comparison is demonstrated to have teeth on its own code path.

## 2.5 Assumptions (all load-bearing, declared, frame-neutral)

1. Finite toy instance; MAXLEN = 4, four stages, machine-checked quiescent at
   stage 4 (trivially provable from the length bound; proof not written; the
   [E] quiescence check extends the horizon to 6 stages). No claim of
   generality beyond the instance.
2. The reconstruction model's hypothesis class H = {0,1,2,3} is a *choice*;
   REC-facts are relative to H. A different declared H changes decidable sets.
   The packet records H as a declared free parameter of the reconstruction
   model and does not adjudicate its status.
3. "Issuance structure fixed under ALPHA" and "projection fixed under BETA" are
   certified by identity of the defining code — that is, by out-of-model access
   to the model text, not through any in-model interface. Recorded as a
   provenance fact about how those certificates are obtained; no classification
   is implied or attached.
4. `Iss_n` issues all admissible candidates deterministically; single observer;
   `Glue_n` present in the signature but not exercised (§2.1).
5. This instance sits below the `OnlineIssuance^LC` gate's clause 4 concerns (a
   fixed-law absorber trivially covers a finite deterministic toy); it is a
   *task-vocabulary and intervention-structure* fixture, not a source-issuance
   witness. It must not be cited as evidence for `Issue[S]` of any type.

## 2.6 Grades

- Fixture computation: **machine-checked** (deterministic stdlib script, run
  2026-07-16, full unabridged output in §2.4, artifact and output content
  hashes pinned above); **not** proof-assistant verified.
- Check discipline: 4 [E] + 6 [F] evidential checks; 1 [T]
  theorem-consequence check listed separately with no evidential weight;
  lints clean under `tests/tef_check_tag_linter.py --strict`.
- Design claims (that ALPHA/BETA constitute a well-formed paired intervention
  over a shared vocabulary): **exploration grade**.
- Physical or source-issuance significance: **none claimed**
  (`Issue[S]^physical: false` unchanged; TI-C019/TI-C020 untouched).

## 2.7 Structural limits (declared)

This family is deterministic and single-observer; the true k lies in every
consistent hypothesis set, so every context-decided REC-fact is a restriction
of the single global ISS profile. A globally consistent assignment therefore
exists **by construction** and restricts correctly to every declared interface
(the family is globally patchable). Any receiver analysis that needs case
families with incompatible interface contexts must look beyond this fixture —
for example to a `Glue_n`-exercising multi-observer variant (§2.8). This is a
neutral structural fact about the fixture's scope, recorded so the fixture is
not over-read in any direction.

## 2.8 Open fields for the TI steward at issuance

- Schema version to issue under (v0.1 vs v0.2 with bundle digests).
- Whether to carry the script as a manifest blob (it is already pinned by
  `content_sha256` above and committed in the authoring repo; TI may re-pin at
  its own path).
- Whether TI wants a `Glue_n`-exercising variant (two observers,
  reconciliation) before or after issuance.
- Final packet ID and placement.

## Deficiency-cure map (v0.1 → v0.2, against TI's 2026-07-16 return)

1. **Native-signature fidelity:** §2.1 now quotes the seven-component RUN-0081
   signature exactly; `Ext_n` and `Glue_n` are in the signature with declared
   instantiated/trivialized status (`Ext_n` represented by `(e, w)` pairs over
   the gate-clause `CandExt`; `Glue_n` present, unexercised); `w_n : Adm_n(e_n)`
   witnesses are explicit per-event fields in `tau_n`, visible in the code, the
   output records, and the structural summary.
2. **Reproducible artifact in hand:** §2.3 inlines the exact committed script
   byte-for-byte (splice-verified at build time); §2.4 is its true unabridged
   stdout; real `content_sha256` values are pinned for both.
3. **Frame-blindness:** all contending-frame citations are removed; former
   assumptions 2 and 3 are restated in frame-neutral language (declared free
   parameter; out-of-model certification provenance fact); the D1 quarantine is
   carried as a frame-neutral authorship restriction in the header.
4. **Failing-direction controls:** the formerly tautological ALPHA-invariance
   check is tagged [T] (holds by construction, no evidential weight, excluded
   from the evidential headline) and is protected by a real [F] probe; six [F]
   perturbation probes flip every substantive comparison; the fixture passes
   the house [T]/[E]/[F] linter in strict mode.

Minor items also applied: "provably quiescent" → "machine-checked quiescent
(trivially provable from the length bound; proof not written)"; the
files-consulted declaration below is written fresh for this re-authoring run
with correct arithmetic.

## Files consulted (this re-authoring run, complete declaration)

- `possibility-to-capability/AGENTS.md`, `governance/CHARTER.md`,
  `steward/README.md` — read in full (governance surfaces).
- `possibility-to-capability/explorations/2026-07-16-northstar-unblock/lane-C-frame-r-and-ti-case.md`
  — read in full (v0.1 draft and its governing referee report).
- `possibility-to-capability/packets/README.md`, `tests/README.md`,
  `tests/tef_check_tag_linter.py` — read in full (placement and house check
  discipline).
- `possibility-to-capability/explorations/2026-07-16-northstar-unblock/SYNTHESIS.md`
  and `explorations/2026-07-16-progress-lanes.md` — runway-context excerpts
  only (grep hits with surrounding lines).
- `temporal-issuance/FORMAL-OBJECT.md` — lines 170–289 (RUN-0081 signature,
  gate, absorber verdict, RUN-0097, RUN-0099), read-only; TI repo pinned at
  commit `3c7d1fc1416576635aca1550e912deb240046ae8`. No TI file edited.
- `CapacityOS/system/mailboxes/possibility-to-capability/20260716-tipit02-not-yet-issuable-deficiency-receipt.md`
  and the archived
  `CapacityOS/system/mailboxes/temporal-issuance/archive/20260716-tipit02-paired-intervention-packet-draft-for-issuance.md`
  (header/receipt portion) — the sovereign return being cured.
- **Not read (blindness preserved):** the receiver-side rival-frame artifacts
  under `experiments/rival-frames/`, the frozen preregistration directory
  `explorations/2026-07-16-rank2-preregistration/`, and every other receiver
  diagnostic or expectation surface. This re-authoring run stayed blind to
  receiver expectations and to every contending frame's current form.
